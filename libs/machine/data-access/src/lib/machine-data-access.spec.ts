import { machineDataAccess } from './machine-data-access';

describe('machineDataAccess', () => {
  it('should work', () => {
    expect(machineDataAccess()).toEqual('machine-data-access');
  });
});
